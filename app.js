// Definindo uma lista de números
let numeros= [2,4,6,8,10]
// Percorrendo a lista com um loop for 
for (let i = 0; i < numeros.lenght; i++){
  // verificando se o número é par ou impar 
  if (numero(i) % 2 === 0) {
    console.log(numero(i) + " é um numero par);
    )else(
      console.log(numero(i)+ " é um numero impar);
   }
}
